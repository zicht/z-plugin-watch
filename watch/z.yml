watch:
#    zwatch: ~
    config: zwatch.json

tasks:
    zwatch.watch:
        help: |
            Watch for changes and execute commands.
        do:
            - ./node_modules/zwatch/bin/zwatch --config $(watch.config)
