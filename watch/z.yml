watch:
#    zwatch: ~
    config: zwatch.json

tasks:
    zwatch.watch:
        help: |
            Watch for changes and execute commands.
        do:
            - $(watch.zwatch) --config $(watch.config)
